package com.topviec.topviec_be.enums.cvs;

public enum FileUploadType {
    CV, // PDF/DOCX — resource_type = raw
    AVATAR, // ảnh đại diện — resource_type = image
    COMPANY_COVER // ảnh bìa công ty — resource_type = image
}
